apiVersion: v1
kind: PersistentVolume

metadata:
  name: pv-todolist

spec:
  capacity:
    storage: 1Gi
  storageClassName: standard
  accessModes:
    - ReadWriteMany
  persistentVolumeReclaimPolicy: Delete
  hostPath:
    path: /data/todoapp
